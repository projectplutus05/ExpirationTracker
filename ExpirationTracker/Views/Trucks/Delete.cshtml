@model ExpirationTracker.Models.Truck
@{
    ViewData["Title"] = "Delete Truck";
}

<h2 class="mb-4 text-danger"><i class="bi bi-trash me-2"></i>Delete Truck</h2>

<div class="card border-danger shadow-sm" style="max-width:500px">
    <div class="card-body">
        <p>Are you sure you want to delete this record?</p>
        <dl class="row">
            <dt class="col-sm-4">Driver</dt><dd class="col-sm-8">@Model.Driver?.DriverName</dd>
            <dt class="col-sm-4">Truck #</dt><dd class="col-sm-8">@Model.TruckNo</dd>
            <dt class="col-sm-4">Make</dt><dd class="col-sm-8">@Model.TruckMake?.Name</dd>
            <dt class="col-sm-4">Model</dt><dd class="col-sm-8">@Model.Model</dd>
        </dl>
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-danger">Delete</button>
                <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
            </div>
        </form>
    </div>
</div>
